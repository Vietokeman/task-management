// ================================================
// Core Layer - Paged Result Model
// ================================================
// PURPOSE:
// Paginated response wrapper.
// Matches backend PagedResult<T> structure.
// ================================================

export interface PagedResult<T> {
  // Define pagination properties:
  // items: T[];
  // pageNumber: number;
  // pageSize: number;
  // totalCount: number;
  // totalPages: number;
}
